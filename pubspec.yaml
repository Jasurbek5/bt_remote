name: bt_remote
description: Bluetooth HID Keyboard & Mouse Remote Controller

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.3.0
  cupertino_icons: ^1.0.6
  permission_handler: ^11.3.1 # Qo'shildi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
